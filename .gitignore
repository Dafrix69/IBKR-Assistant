# 本地配置含账号别名与真实账号映射,永远不进仓库(§9.2)
config/settings.json
config/*.local.json

# 桌面端:依赖、打包产物、预览与压测输出
desktop/node_modules/
desktop/dist/
desktop/build/engine-ts/
.uipreview/

# 引擎自己的忽略见 engine-ts/.gitignore
node_modules/
.claude/
.DS_Store

# 某次把 ~ 当成家目录建出来的数据目录,里面是真实交易记录(S1),不进仓库
~/
