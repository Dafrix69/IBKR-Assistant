# 同时向两个账户发单

指令输入框下面有一排「发单账户」勾选(只有配置了两个以上账户时才出现)。勾一个,单子发到那个
账户;**勾两个,每笔订单各发一份**——引擎在模型解析之后、硬校验之前按勾选账户把订单复制一遍,
每一份独立走限额、实盘闸门、重复防抖,独立落库、独立下单,结果卡片上按账户分别显示。

几条边界:

- 指令里明确点名账户的订单(如「用模拟账户买…」)不复制,照用户写的发;只有模型填
  `account="DEFAULT"` 的订单才扇出。
- `allow_live_trading=false` 时勾上实盘账户也没事:实盘那份会以 `LIVE_TRADING_DISABLED`
  被拦,纸面那份照常——正好用来在模拟盘上先跑一遍同一条指令。这件事本地就知道,所以勾选行下面
  直接写出来(「『主账户』是实盘账户,当前没有允许实盘下单……」+ 一个「去设置」),不必等解析完
  才由拒绝卡片告诉用户;**只提醒,不替用户改勾选**。只配了一个实盘账户、勾选控件根本不出现时也照样提醒。
- 单次输入的订单上限按账户数放大(`max_orders_per_input × 账户数`),否则第二个账户的单
  会被当成超额拦掉。重复防抖的指纹含账户别名,同一笔单发到两个账户不会互相当成重复。
- 勾选状态记在本机;没勾过时默认只勾默认账户,行为与没有这个控件时一致。
- 命令行同样支持:`parse` / `run` 加 `--accounts 模拟,主账户`。

两个 TWS 实例(如 7496 个人账户、7497 公共账户)各自登录,配置里 `connections` 一条端口对
一个实例,`accounts` 用别名把账号挂到对应连接上;连接时引擎会核对别名账号是否真的在该实例
可管的账户列表里,登录切换后对不上会直接拒单而不是落到错误账户。

## 止盈策略回放(《SPX 0DTE 蝶式止盈策略》v2.1)

同一张蝴蝶的开仓与平仓成交自动配对(平仓那条在列表里标「归入 xx 那张开仓」,分析它等于分析开仓单)。
分析结果里除了 SPX 的 K 线,还有**蝶价走势**:IBKR 对组合合约给历史分钟中间价(已到期的腿带 `includeExpired`
才能 qualify),`combo_bars` / `comboBars` 拉当天一整天;拿不到的分钟用 Bachelier 正态模型价(σ_剩余)补,图上虚线。

策略逻辑在 `flyexit.ts`(黄金对拍 `flyexit.json`,含 9-03 那笔真实交易的回放)。

**止盈 = 浮盈回撤追踪(v2.1 起)。** 蝶价 ≥ 1.3×D 之后开始记浮盈(蝶价 − D)高水位,从高水位回吐掉当前
档位的比例就全清。档位按浮盈分级收紧——蝶式的盈利有硬天花板(最大值 = 翼宽 W),越靠近天花板剩余上涨
空间越小、下跌风险越大:浮盈 < 1×D 让 40%、1–3×D 让 30%、≥ 3×D 让 20%,15:00 之后一律再乘 0.5。
两道闸:回吐不足 `trail_floor`(默认 0.20 点,约一个组合价差)不触发,免得在中间价噪声里被扫;位置信号
一律优先于价格信号(标的出 0.45W 时按区间规则归因,阶段 C 整段让给区间规则)。

**回撤按浮盈算,不是按蝶价算。** 同样叫"回撤 30%",两种口径能差出一倍以上的让利:D = 2.25、高点 8 时,
浮盈口径在 6.28 出场,蝶价口径要跌到 5.60 才出。

**固定倍数档位(1.35×D / 1.70×D)默认关闭**,传 `tp1` / `tp2` 可恢复 v2.0 的分批止盈,和追踪并存。
关掉的原因:蝶价的上限是翼宽,用 D 的倍数封顶会在 D 小的时候锁死出场——9-03 那张 D = 2.25、W = 20 的单,
1.35×D 只有翼宽的 15%,蝶价当天摸到 8.15,固定档位在 3.30 就清光了(+105),换成追踪拿到 5.75(+350)。

其余规则照抄文档:阶段 B(14:00–σ_剩余 = W/1.6)|S−K| > 0.45W 全清、14:00 留仓压到 1/3;阶段 C
|S−K| < 0.45W 持到结算、0.45–0.55W 清一半、更远清残值;任意时刻蝶价 ≤ 0.5×D 或出 0.8W 且剩余方差 > 30% 止损。
σ_剩余 = EM × √R,R 按文档 §3 的半小时方差权重,EM 默认 36,界面上可改(文档要求每日从 ATM straddle 取)。

几条落地口径:张数不足 3 张时"清 1/3"按 max(1, n//3);**所有分批规则至少留 1 张**——14:00 的留仓上限和
远端 OTM 蝶(文档 §2.3)进入 K±10 的分批落袋,在 1 张仓位下都会算出 0 张、把整套止盈变成一次性清仓,
所以都下了地板。开仓时就在 0.8W 之外的远端 OTM 蝶,位置止损从标的首次进入 0.8W 之后才生效。
触发判断只有中间价(文档要求按 bid),回放结果略偏乐观,说明里写着;高水位尤其吃亏,`trail_floor` 就是为此留的余量。

**这几个档位还没回测过。** 现在的 40/30/20 是按蝶式的天花板效应和 0DTE 的衰减速度推出来的起点,不是拟合出来的。
单日扫描只能读出噪声(9-03 那天 20% 比 25–35% 更好,≥40% 干脆没触发),要定参得攒够几十个交易日的组合分钟线。

输出:蝶价图上的止损 / 回撤激活线 + 随高水位棘轮上移的**回撤触发价阶梯线**,SPX 图上的 ±0.45W / ±0.55W / ±0.8W
临界线,逐分钟回放的出手事件(时间、阶段、张数、价格、盈亏、触发的规则),以及「按策略 / 实际 / 持到结算 /
最高蝶价 / 浮盈高水位」五种结局对比。
